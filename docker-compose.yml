version: '3'

services:
  app:
      build:
        context: .
        dockerfile: App.Dockerfile
      ports:
        - 5000:5000
      restart: always

  test:
    build:
        context: .
        dockerfile: Chrome.Dockerfile
    depends_on:
      - app
